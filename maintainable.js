//Blank line after for/while/if

// Two indedents before the break line

// Declare any local viriable at the top of the function

//Combine all local viriable with one var

// Avoid == and !==, try to use === and !===. Because 5 == '5'(valueof, toString)

//Avoid to use primitive wrapper types(String, Boolean, Number)

//Check property: prop in obj
